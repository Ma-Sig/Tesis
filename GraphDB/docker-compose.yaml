services:
  graphdb:
    image: ontotext/graphdb:10.4.0  
    container_name: graphdb_oasis
    ports:
      - "7200:7200"
    volumes:
      - graphdb_data:/opt/graphdb/home
    restart: unless-stopped

volumes:
  graphdb_data: